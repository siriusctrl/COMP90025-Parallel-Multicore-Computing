#include <iostream>
#include <vector>
#include <algorithm>

using std::vector;
using std::cin;
using std::cout;
using std::endl;

void find_solution(int n, int max_solution);
bool check_acceptable(const vector<int> &queen_rows);